.DS_Store
*.log
node_modules/
.vscode/
.claude/
*.tmp
dist/
dist-android/
# The Android native project itself is committed (Capacitor's own
# convention — it can carry real hand-written native changes), but not its
# own generated build output/caches, nor the copy of the web app that
# `cap sync` recreates from web-dist/ on every build.
web-dist/
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/build/
